import type { Config } from "@react-router/dev/config";

export default {
  appDirectory: "src",
  ssr: false,
  basename: "/cindertide-studio-site/",
  routeConfigFile: "routes.ts",
} satisfies Config;
